import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  itemContainer: {
    color: '#ffffff',
    fontSize: 20,
    margin: 15
  },
  itemText: {
    marginBottom: 15,
    fontSize: 15,
  }
});
